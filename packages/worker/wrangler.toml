#:schema node_modules/wrangler/config-schema.json
name = "fbg-worker"
main = "src/index.ts"
compatibility_date = "2025-01-15"
compatibility_flags = ["nodejs_compat"]

# Pin the Cloudflare account so CI (non-interactive) can pick when the
# token happens to have access to more than one account. This ID is not
# a secret — it's in every wrangler log — so hardcoding is fine.
account_id = "11a15d968a6aefccfcd9e6dabb7be231"

# One Durable Object class per game room.
[[durable_objects.bindings]]
name = "GAME_ROOM"
class_name = "GameRoom"

# DO classes need a migration tag the first time they're deployed.
# Free plan requires SQLite-backed DOs (new_sqlite_classes). Paid plans
# can also use the legacy key-value store (new_classes); SQLite is the
# modern default and what we're using for engineState + action log.
[[migrations]]
tag = "v1"
new_sqlite_classes = ["GameRoom"]

# Serve the FBG static client from ../../public on the same domain. Single
# origin means the client WebSocket URL is just wss://<worker-host>/api/ws.
[assets]
directory = "../../public"
binding = "ASSETS"

# Local dev defaults: wrangler dev serves on 8787 by default.
[dev]
port = 8787
